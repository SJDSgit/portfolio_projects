Performed data cleaning on a raw Nashville Housing Dataset and made it more fitting for exploration. 
Data Cleaning was performed on Microsoft SQL Server.

Tasks

• Standardized data format (sale date) by converting from date/time format to ordinary date format

• Cleaned property address data (populating null value) by joining the table to itself (i.e., if x=y, then y=x)

• Segregated address into individual columns (Address, City, State) also deleted unused column using (DROP Command)

• Converted 'y' and 'n' to yes or no in (sold as vacant) field... etc
